{
  "name": "augrun-test-ui",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "//": "Scripts invoke node directly instead of relying on node_modules/.bin being on PATH. The repo lives under a mount point containing a colon (…-0:0-part1), and PATH is colon-separated, so npm's PATH entry for .bin is split into two invalid paths and no binary resolves by name. Explicit paths sidestep that entirely.",
  "scripts": {
    "abis": "node scripts/extract-abis.mjs",
    "dev": "node scripts/extract-abis.mjs && node node_modules/vite/bin/vite.js",
    "build": "node scripts/extract-abis.mjs && node node_modules/vite/bin/vite.js build",
    "preview": "node node_modules/vite/bin/vite.js preview"
  },
  "dependencies": {
    "@tanstack/react-query": "^5.59.0",
    "react": "^18.3.1",
    "react-dom": "^18.3.1",
    "viem": "^2.21.0",
    "wagmi": "^2.12.0"
  },
  "devDependencies": {
    "@types/react": "^18.3.0",
    "@types/react-dom": "^18.3.0",
    "@vitejs/plugin-react": "^4.3.0",
    "typescript": "^5.6.0",
    "vite": "^5.4.0"
  }
}
